# sgs-love 标准版补齐 · 开发计划

> **进度（2026-09-14）**：T1/T2/T3/T4/T5/T6/T7/T8/T10/T11/T12/T13 已完成，
> T9 已完成 6/8 项。测试 354 → 418，压测 6 组全通过。
> 剩余项见文末「未完成」。

> 依据：`AUDIT-标准版对标.md`（对照《三国杀基础版武将与卡牌全表》《三国杀身份局游玩说明》）
> 适用范围：`/Users/liming/Code/AIGame/sgs-love`
> 原则：**先规则正确性，再标准版一致性，最后语义细节**；每个任务都必须带断言型测试。

---

## 0. 基线与工作方式

### 0.1 现状基线（改动前）

| 项 | 值 |
|---|---|
| 测试 | 核心 354 / UI 12 / 网络 33，`./run-tests.sh` 全绿 |
| 压测 | `./run-soak.sh`（60 将逐将覆盖 + 多种子 + 卡牌守恒） |
| 武将 | 60 将（蜀15 魏15 吴15 群15） |
| 牌堆 | 118 张（标准 108 + 军争混堆） |

### 0.1b 当前状态（2026-09-14 复核：T1–T7 **全部完成**）

| 任务 | 内容 | 状态 | 提交 |
| --- | --- | --- | --- |
| T1 | 濒死救援：允许其他角色出【桃】 | ✅ | `2250e6a` |
| T2 | 主公技三件套（护驾/激将/救援）+ 主公选将 | ✅ | `2250e6a` |
| T3 | 补齐 5 张武器（3 张新建 + 2 张补效果） | ✅ | `2250e6a` |
| T4 | 实现【观星】（诸葛亮） | ✅ | `4e21247` |
| T5 | 陆逊【连营】（替换【度势】） | ✅ | `4e21247` |
| T6 | 固定花色点数表 + 4 张 EX 牌 | ✅ | `4fe391a` |
| T7 | 数值与配置修正 | ✅ | `2250e6a` / `7f2c639` |

完成后基线（实测）：

| 项 | 值 |
|---|---|
| 测试 | **核心 418 / UI 12 / 网络 45**，压测 6 组，全绿 |
| 武将 | 60（标准版 25 + 扩展 35）；25 将总表已由断言锁死 |
| 技能条目 | 120 |
| 卡种 | 39（基本 8 / 锦囊 16 / 装备 15，武器 9） |
| 牌堆 | **108 张**，标准版预设、花色点数固定 |

### 0.2 每个任务的完成定义（DoD）

1. 代码实现 + **新增断言型单测**（不是"能跑"，是"断言到了正确行为"）
2. `./run-tests.sh` 全绿
3. `./run-soak.sh` 全绿（涉及武将/卡牌/规则的必跑）
4. 同步更新 `README.md` / `DESIGN.md` 里受影响的表述（**具体数字必须一起改**，见历史教训 34）
5. 提交（账号 `catsonkeyboard <leemeany@gmail.com>`，见仓库 git config）

### 0.3 新增内容必须同步的位置（踩过的坑，每次都要检查）

| 新增什么 | 必须同步 |
|---|---|
| 新卡牌 | `cards.lua` 定义 → `standard.lua` 牌堆配比 → `bot.lua` 的 `PLAY_PRIORITY` / `CONVERT_TARGETS` / 装备估价表 → `skin.lua` 的 `CARD_CAMEL`（装备是 CamelCase 卡图） → `tests/test_game.lua` 的"关键卡牌定义齐全"白名单 |
| 新武将/技能 | `generals.lua` → 跑**逐将覆盖**压测（随机抽样会漏，历史教训 2） |
| 新询问类型 | `bot.lua` 的响应分支 → `src/compat/api.lua` 别名 → `tests` 的 `runInRoom(fn, respond)` |
| 引擎新事件 | `skill.lua` 的 `TriggerEvent` → 涉及"以他人为主语广播"的事件，技能回调**必须**判 `data.player ~= player`（历史教训 1） |

---

## P0 · 规则正确性（影响对局是否成立）

### T1 濒死救援：允许其他角色出【桃】

- **现状**：`src/core/room.lua:1407 Room:_dying` 只向濒死者本人 `askForCard(p, "peach")`，无人响应即 `_kill`。文档要求"濒死者本人优先，之后按逆时针依次询问所有角色"。
- **改动点**：`room.lua:_dying`
  1. 保留"本人优先"一轮（本人可用【桃】或【酒】）
  2. 之后按 `self.players` 从 `current_seat` 起逆时针轮询其余存活角色，逐个 `askForCard(q, "peach", "是否用【桃】救 <p>？")`
  3. 任一桃使 `p.hp` 回到 1 以上即 `trigger("QuitDying")` 退出
  4. 【完杀】分支保持（他人不能出桃）
  5. 注意：`Room:heal` 已保证不超上限；救活后要走 `QuitDying`
- **验收**（新增测试，放 `tests/test_game.lua`）
  - 本人无桃、他人有桃 → 不应阵亡，体力回到 1
  - 本人与他人均无桃 → 阵亡
  - 【完杀】生效时他人有桃也救不回
  - 多个救援者时只消耗刚好够的桃数（不多弃）
- **风险**：`askForCard` 是 `coroutine.yield`，测试必须包在 `runInRoom` 里（历史教训 3）；人类玩家路径要确保 UI 能收到非当前行动者的请求（`src/ui/scene_room.lua` 的 `pending.player` 判断）。

### T2 主公技三件套 + 主公选将

- **现状**：全库 0 处实现（继承国战版 Hegemon 源码缺失）。
- **改动点**
  - `generals.lua`：曹操加【护驾】、刘备加【激将】、孙权加【救援】，均标记 `frequency = Freq.Lord`（若 `skill.lua` 的 `Frequency` 没有 `Lord`，先补一个），并加 `lord_only = true`
  - `room.lua` 新增 `Room:askForLordSkill(lord, card_name, kingdom)`：
    - 由主公发起，对**所有同势力存活角色**逐个 `askForCard(q, card_name, "是否为 <主公> 提供【X】？")`
    - 有人响应 → 该牌进弃牌堆，视为主公使用/打出
  - 【护驾】：主公需出【闪】时（被【杀】/【万箭齐发】），先走 `askForLordSkill(lord, "dodge", "wei")`；成功则视为已出闪
    - 挂点：`_resolveSlash` 的 `askForCard(to,"dodge")` 之前；`cards.lua` 的 `archery_attack` 里同样处理
  - 【激将】：主公需要使用【杀】时，走 `askForLordSkill(lord, "slash", "shu")`；成功则生成一张 `phantomCard("slash")`（**必须 phantom**，否则卡牌守恒会破，历史教训 8）
    - 挂点：`_useBasic` 的杀分支与 BOT 出牌路径
  - 【救援】（锁定技）：吴势力角色对主公使用【桃】时额外回复 1 点
    - 挂点：`Room:heal` 之外，最好在 `_dying` 的救援分支里判断 `rescuer.kingdom == "wu" and p.role == "lord"` → `heal(p, 2)`
  - `room.lua:setupRoles` 或开局流程：主公从"带主公技的武将 + 随机 3 张"中选（文档第 4 节）。**至少**保证主公池优先给曹/刘/孙
- **验收**
  - 主公技只在 `p.role == "lord"` 时可用（非主公身份不触发）
  - 无同势力角色在场时不触发
  - 【救援】：吴势力桃救主公 → +2；非吴势力 → +1
  - 卡牌守恒：`--soak` 的守恒校验不能报警（phantom 牌不可被【奸雄】收走）
- **依赖**：T1（【救援】依赖濒死救援轮询的存在）

### T3 补齐 5 张武器（3 张新建 + 2 张补效果）

- **现状**
  - 完全未定义：雌雄双股剑 `double_sword`、青龙偃月刀 `blade`、方天画戟 `halberd`
  - 有定义无效果：丈八蛇矛 `spear`（`room.lua` 从未引用）、贯石斧 `axe`（同）
  - 卡图资源 `assets/image/card/{DoubleSword,Blade,Halberd,Spear,Axe}.png` **都已存在**
- **改动点**
  1. `cards.lua`：补 `double_sword`（范围 2）、`blade`（范围 3）、`halberd`（范围 4）定义
  2. `room.lua:_resolveSlash` / `_slashHit` 补效果：
     - **雌雄双股剑**：【杀】指定异性角色后，目标先弃 1 张手牌，否则使用者摸 1 张
     - **青龙偃月刀**：目标出【闪】后，可对其**再使用** 1 张【杀】（走 `_resolveSlash` 递归，注意加深度上限防死循环）
     - **方天画戟**：【杀】结算后若无手牌，可额外指定至多 2 名角色
     - **丈八蛇矛**：可将 2 张手牌当【杀】使用或打出 → 需要 `pairViewAs` 型转化（参考 `乱击` 的 `s.n == 2 and s.filter_pair` 分支，`room.lua:506` 已支持）
     - **贯石斧**：【杀】被【闪】抵消时，可弃 2 张牌令此杀依然造成伤害
  3. `standard.lua:DECK_SPEC` 按文档配比加入（各 1 张）
  4. `bot.lua` 装备估价表已含这 5 张（第 92-93 行），无需改；但 **丈八蛇矛的转化要登记 `CONVERT_TARGETS`**（历史教训 5）
  5. `tests/test_game.lua` 的"关键卡牌定义齐全"白名单补齐全部 9 张武器
- **验收**：每张武器一条断言型用例（雌雄：异性目标手牌 -1 或使用者手牌 +1；青龙：出闪后可再出杀；方天：空手牌时多指定 2 人；丈八：2 手牌 → 1 张杀；贯石：弃 2 牌强命）

---

## P1 · 标准版一致性

### T4 实现【观星】（诸葛亮）

- **现状**：`generals.lua:166` 只有【空城】；`compat/api.lua:359` 留了注释说"本引擎没有该交互界面"
- **改动点**
  - `room.lua` 新增 `Room:askForGuanxing(p, cards)`（协程 yield，返回重排后的牌序）；兼容层已有同名桩，**要删桩改为委托**（`define()` 撞名守卫会拦，历史教训 23）
  - `generals.lua`：准备阶段（`EventPhaseStart` + `phase == "start"`）发动，看牌堆顶 `X = min(存活人数, 5)`
  - 无交互时（BOT/headless）默认保持原序
- **验收**：观星后牌堆顶顺序 = 返回的顺序；牌数守恒；BOT 不崩

### T5 陆逊【连营】（替换【度势】）

- **现状**：装的是国战版【度势】；标准版应为【谦逊】+【连营】。
- **改动点**
  - `generals.lua:1051` 陆逊：`singleViewAs("度势", ...)` → 改为【连营】"失去最后一张手牌时，可以摸一张"
  - **引擎挂点**：目前 `CardsMoveOneTime` + `last_handcard` 只在 `_phase_discard`（`room.lua:684`）触发，覆盖不到出牌/被顺/被拆/响应出闪
    → 新增 `Room:notifyHandEmpty(p)`（内部 trigger `CardsMoveOneTime` with `last_handcard`），并在这些**收口处**调用：
    `Room:useCard` 末尾、`_resolveSlash` 出闪后、`cards.lua` 的 `snatch` / `dismantlement` / `savage_assault` / `archery_attack` / `duel` 响应后、`askForDiscardFrom`
  - 注意【死谏】（`generals.lua:1780`）也挂在同一事件，会一起被扩到更多时机 —— **这符合原版语义**，但要确认压测不因此失衡
- **验收**：出牌打到空手 → 摸 1；被顺到空手 → 摸 1；手牌非空时不触发；`--soak` 卡牌守恒

### T6 固定花色点数表 + 4 张 EX 牌

- **现状**：`standard.lua:58 buildDrawPile` 用 `suits[rng(4)], rng(13)` 随机生成；不保证每花色 27 张；无 EX 牌
- **改动点**
  - 新建 `src/core/deck_spec.lua`（或在 `standard.lua` 内建表）：按文档第 8 节录入 108 条的「花色 × 点数 → 牌名」固定表
  - `buildDrawPile` 改为先按表生成实体牌（花色点数固定），再用 LCG 洗序（保留可复现性）
  - 4 张 EX 牌：♠2 寒冰剑、♣2 仁王盾、♥Q 闪电、♦Q 无懈可击
  - 提供 `Standard.buildStandardPile()`（纯 108）与现有 118 混堆并存的开关，默认先切到 108（与文档对齐）
- **验收**：`deckSize() == 108`；每花色 27 张；♥3/4/6/7/8/9/Q 是桃；♦2-10/J 是闪；`--soak` 守恒

### T7 数值与配置修正

| # | 项 | 文件:位置 | 改法 |
|---|---|---|---|
| a | 关羽 5 → 4 血 | `generals.lua:158` | 改 `max_hp = 4`（若决定对标国战版则改文档，二选一，**先确认**） |
| b | 吕布 5 → 4 血 | `generals.lua:1462` | 同上 |
| c | 主公 +1 体力条件化 | `room.lua:105` | `#players >= 5` 才 `max_hp + 1` |
| d | 9/10 人身份配置 | `room.lua:73 ROLE_SETUP` | 补 `[9] = {lord1, loyal3, rebel4, reneg1}`、`[10] = {lord1, loyal3, rebel4, reneg2}` |
| e | 判定区 LIFO | `room.lua:630 _phase_judge` | 改取 `p.judges[#p.judges]`（最后放入的先判） |
| f | 判定区同名唯一 | `room.lua:870` / `player.lua:166` | `addJudge` 前校验 `hasDelayed(card.name)`，已存在则退还（`_refund`）并提示 |

- **验收**：4 人局主公不加体力；9/10 人局身份数正确；先放的【乐】后判；同一角色无法被贴第二张【乐】

### T8 牌堆切到标准 108（可与 T6 合并）

- `DECK_SPEC` 调整：无懈可击 3 → 4、闪电 1 → 2；移出军争牌（酒4/火攻3/铁索4/兵粮2/白银狮子1/藤甲1）；
  30 杀是否保留"24 普杀 + 3 火杀 + 3 雷杀"由设计决定（文档是 30 张普通杀）
- **注意**：移出【酒】后 `_useBasic` 的 analeptic 分支会被闲置，保留即可；但 `silver_lion`/`vine` 的效果代码也在，同样保留

---

## P2 · 语义细节

### T9 技能语义修正（8 项，可拆成小提交）

| 技能 | 文件:位置 | 现状 | 目标 |
|---|---|---|---|
| 鬼才 | `generals.lua:425` | 只在自己判定会命中时改判 | 去掉 `data.player == player` 限制，改为"可主动改任意角色的判定"（BOT 保留保守策略，人类可主动选） |
| 仁德 | `generals.lua:141` | 累计 3 张回血、跨回合累计 | 本出牌阶段内首次给出 ≥2 张即回 1 血，`EventPhaseEnd(play)` 重置计数 |
| 苦肉 | `generals.lua:964` | 每回合限一次 | 去掉 `kurou_used`，改为每阶段可多次（BOT 保留 `hp < 3` 的保守策略） |
| 结姻 | `generals.lua:1063` | 要求自己也已受伤 | 只要求目标是"已受伤的其他男性角色" |
| 反间 | `generals.lua:992` | 引擎随机花色 | 新增 `askForChoice(t, {"♠","♥","♣","♦"})` 询问，先猜后展示；BOT 随机选 |
| 离间 | `generals.lua:1469` | 决斗可被无懈 | 生成的 `phantomCard("duel")` 置 `card.no_nullify = true`，`room.lua:862` 的 `askForNullification` 前判断 |
| 遗计 | `generals.lua:540` | 自己摸 2 张 | （可选）改为可分配；若不做，保留注释即可 |
| 武圣 | `generals.lua:159` | 只转化手牌 | 扩展到装备区的红色装备（`viewAsCandidates` 的候选池加装备；注意"当杀时攻击范围按 1 算"） |

### T10 【酒】语义

- `room.lua:971`：出牌阶段使用只置 `from.drunk = true`（给下一张杀 +1）；**只有濒死时**才 `heal(1)`
- 验收：出牌阶段满血/受伤时饮酒都不回血，且下一张杀伤害 +1；濒死时饮酒回 1 血

### T11 平局判定

- `room.lua:437 drawCards`：`drawPile` 与 `discardPile` 均为空时无法满足摸牌 → 置 `game_over`、`winner = nil`（平局）
- 验收：构造空牌堆场景，对局以平局结束而非继续或崩溃

### T12 UI 卡图映射修复（顺带发现）

`src/ui/skin.lua:113 CARD_CAMEL` 三处映射与实际文件名不符，实测均返回 nil：

| 内部名 | 现在解析成 | 实际文件 | 修正 |
|---|---|---|---|
| `dodge` | `Dodge.png` | `jink.png` | `CARD_CAMEL.dodge = "Jink"` |
| `qinggang_sword` | `QingGang.png` | `QinggangSword.png` | `"QinggangSword"` |
| `ice_sword` | `Ice_sword.png` | `IceSword.png` | `CARD_CAMEL.ice_sword = "IceSword"` |

- 验收：脚本遍历全部卡牌定义，`cardImage()` 无 nil（可加进 UI 测试）

### T13 测试加固：防止"白名单腐坏"

- `tests/test_game.lua:1931`「关键卡牌定义齐全」白名单 → 改为**直接遍历 `Standard.DECK_SPEC` 用到的全部牌名**（或导出 `DECK_SPEC`），不再手写名单
- 新增断言组：
  - `Standard.deckSize() == 108`（T8 之后）
  - 25 名标准将的名字、势力、体力、技能名齐全（把审计用的 `check_std25.lua` 逻辑固化成测试）
  - 三个主公技存在且 `lord_only`
  - 濒死时他人可出桃（T1 的用例）
- 目的：让"与文档对齐"这件事本身被 CI 守护（历史教训 35）

---

## 执行顺序建议

```
T1 濒死救援 ──┬── T2 主公技（依赖 T1 的救援分支）
              │
T3 五张武器 ──┴── T6 固定花色表 ── T8 牌堆 108
              │
T7 数值/配置修正（独立，可并行）
              │
T4 观星 ── T5 连营（都要动引擎询问/事件，建议连做）
              │
T9 技能语义 8 项 ── T10 酒 ── T11 平局 ── T12 卡图 ── T13 测试加固
```

**建议提交切分**（每个一次提交，便于回滚）：
1. `T1` 濒死救援
2. `T3a` 三张武器定义 + `T3b` 丈八/贯石效果
3. `T2` 主公技三件套
4. `T7` 数值与配置修正（a~f 可合成一次）
5. `T6+T8` 固定花色表 + 108 牌堆
6. `T4+T5` 观星 + 连营
7. `T9` 技能语义（可再按技能拆 2~3 次）
8. `T10+T11+T12` 酒 / 平局 / 卡图
9. `T13` 测试加固 + 文档同步

---

## 未完成（下一步）

| 项 | 说明 | 备注 |
|---|---|---|
| T2-a 主公选将流程 | 开局「主公从带主公技的武将 + 随机 3 张里选」的交互未做；`Standard.randomGeneral` 目前也没有调用方 | 需先确认 UI 选将入口在哪 |
| T9 遗计 | 仍是「自己摸 2 张」的简化版；完整版是看牌堆顶 2 张并可拆分分配 | 需要 askForYiji 的分配交互 |
| T9 武圣装备区 | 【武圣】只转化手牌，不含装备区的红色装备 | 涉及装备转化后的卸下流程，需谨慎 |

### 两个决策已按「对标标准版」拍板

1. 关羽 / 吕布 → **4 体力**（对齐文档与实体标准版，而非 Hegemon 源码的 5）
2. 牌堆 → **默认 108 张标准版**；`extended`（+军争）作为可选预设保留

1. **关羽/吕布体力**：改代码为 4（对齐实体标准版与文档），还是保留 5（对齐参照的 QSanguosha-Hegemon 源码）并在 README 明确"本引擎对标国战版"？
   → 影响 T7-a / T7-b 是否执行。
2. **牌堆口径**：切回纯标准版 108，还是维持 118（标准+军争混堆）并只补齐缺失的五张武器？
   → 影响 T6 / T8 的范围。若保留 118，则 T6 的固定花色表需要额外给出军争部分的花色点数（原版 `strategic-advantage.cpp` 有）。

未拍板前，建议先做不依赖这两个决策的 **T1 / T3 / T2**（纯增量、无口径争议）。

---

## 工作量与成本估算

> 口径：1 轮 ≈ 一次「读代码 → 改 → 跑测试 → 看结果」循环。
> 实测 `run-tests.sh` 1.05s、`run-soak.sh` 1.21s，**测试本身几乎不占时间**，
> 时间与 token 主要花在「理解现有代码 + 写改动 + 调试回归」上。

| 任务 | 轮次 | Token（低–高） | 墙钟（低–高） | 主要成本来源 |
|---|---|---|---|---|
| T1 濒死救援 | 2–3 | 25k–50k | 10–20 min | `_dying` 改造 + 4 条用例 |
| T2 主公技三件套 | 3–5 | 60k–120k | 30–60 min | 新询问 API + 3 个挂点 + 兼容层删桩 |
| T3 五张武器 | 4–6 | 100k–180k | 45–90 min | 5 个效果（青龙递归、方天多目标最麻烦） |
| T4 观星 | 3–4 | 45k–85k | 20–40 min | 新询问类型 |
| T5 连营 | 3–5 | 60k–110k | 30–60 min | 收口点散（8 处）+ 死谏/枭姬回归 |
| T6 固定花色表 | 4–6 | 90k–160k | 40–80 min | 108 行数据录入与逐条核对 |
| T7 数值/配置修正 | 3–4 | 45k–85k | 20–40 min | 6 个小项分散 |
| T8 牌堆切 108 | 2–3 | 25k–50k | 10–25 min | 与 T6 合并可省一半 |
| T9 技能语义 8 项 | 5–8 | 90k–170k | 45–90 min | 8 个小项 + 各自用例 |
| T10 【酒】语义 | 1–2 | 12k–25k | 5–12 min | — |
| T11 平局判定 | 1–2 | 12k–25k | 5–12 min | — |
| T12 卡图映射 | 1–2 | 12k–22k | 5–10 min | 3 行改动 |
| T13 测试加固 | 3–4 | 40k–80k | 20–40 min | 固化 25 将/108 张断言 |
| **合计** | **35–50** | **≈ 620k–1.16M** | **≈ 4.8–9.7 h** | |

### 计入现实损耗后的预期

| 损耗项 | 系数 | 说明 |
|---|---|---|
| 会话切换重读上下文 | +15%~25% | 新会话要重读 `room.lua`/`generals.lua` 的相关段落 |
| 回归调试 | +20%~30% | 本项目历史上"改一处炸三处"频发（兼容层撞名、卡牌不守恒、点号/冒号错配） |
| 决策返工 | 0 或 +30% | 若关羽/吕布体力、108 vs 118 先没定，T6/T7/T8 可能重做 |

**现实预期：约 75 万 – 150 万 token，6 – 12 小时墙钟，跨 3–6 次会话。**
若两个决策先拍板、且每次会话打包 2–3 个同类任务，可压到 **≈ 80 万 token / 6–7 小时**。

### 降本建议

1. **先拍板两个决策**——返工是唯一会翻倍的成本项。
2. **同类任务打包进同一次会话**：T10+T11+T12 一起（≈35k）、T4+T5 一起（共用一个"新增询问/事件"上下文）、T6+T8 一起。
3. **T6 的花色表让我一次性写完再校验**，不要边写边 diff——数据录入反复核对最费 token。
4. **每任务结束立即提交**，避免长会话上下文膨胀把同一份 `room.lua` 反复送进上下文。
5. 用例复用 `tests/test_game.lua` 已有的 `runInRoom(fn, respond)` 基建，别新造。

### 费用换算

费用 = 实际 token × 你的单价。若按 ¥30 / 百万 token 粗估 ≈ **¥25–¥45**；
按 ¥100 / 百万 token ≈ **¥75–¥150**。（仅为换算示例，取决于你的实际计费方案。）
