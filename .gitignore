# LÖVE 便携版是二进制且体积大，不入库
tools/love.app/
tools/love-win64/
tools/*.zip

# 注意：tools/ 下的**脚本**必须入库。
# 以前这里写的是 tools/（整目录忽略），导致 lint_methods.py 从未被跟踪，
# clone 之后 ./run-tests.sh 会因为找不到它而失败。
*.zip
.DS_Store

# 本地 agent 会话日志（任务输出等），不应入库
.pi/
# 本地工具配置（忽略清单由工具自己维护），同样不入库
.pi-lens.json
# agent 会话产物（规划文件等），不入库
.zcode/

# AI 接口本地配置（含密钥），不入库；模板见 ai.env.example
ai.env
__pycache__/
